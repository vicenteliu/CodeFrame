# Scripts

This folder is reserved for automation scripts that run outside the main Python
package.

- `freecad/`: future scripts for generating CAD geometry and drawing exports.
- `blender/`: future scripts for generating 3D models, scenes, and renders.

Scripts should read structured project inputs and write generated artifacts to
`outputs/`.
